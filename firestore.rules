rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /certificates/{allPaths=**} {
      // Allow the public to view/download the certificate PDF
      allow read: if true;
      // Only your Admin SDK (the Render Backend) can upload/delete
      allow write: if false; 
    }
  }
}