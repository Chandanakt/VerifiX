rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 🔓 ALLOW EVERYTHING (EMULATOR ONLY)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
